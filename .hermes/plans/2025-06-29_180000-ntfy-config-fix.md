# Conserto da Config ntfy — Plano de Implementação

> **Para Hermes:** Use subagent-driven-development skill para executar task por task.

**Goal:** Fazer o `core/ntfy_client.py` respeitar `NTFY_URL` e `NTFY_TOPIC` do `.env`, deduplicar os arquivos legados que duplicam a lógica em `instagram_scraper/`, e validar com um teste de envio real.

**Architecture:** O cliente central (`core/ntfy_client.py`) vira a única fonte de verdade. As 4 cópias legadas em `instagram_scraper/` passam a importar de `core.ntfy_client` em vez de redefinir `NTFY_TOPIC`/`NTFY_URL` como literal `"sentinela"`/`"https://ntfy.sh/sentinela"`. O `NTFY_URL` do `.env` finalmente passa a ser respeitado (hoje o client ignora e reconstrói a URL hardcoded). Mantemos a API pública inalterada: `send_notification(title, message, tags, priority) -> bool` e `EventBus().notify(...)` continuam funcionando.

**Tech Stack:** Python 3.11, `requests`, `pytest`, `python-dotenv` (já presente).

---

## Contexto

Estado atual mapeado:

- `core/ntfy_client.py:9-10` — usa `os.getenv("NTFY_TOPIC", "sentinela")` e monta URL como `https://ntfy.sh/{topic}`, **ignorando** a variável `NTFY_URL` do `.env.example:52`.
- `core/event_bus.py:113-118` — `EventBus().notify()` delega corretamente para `send_notification`. OK.
- `workers/ai/sa_instagram_stealth.py:60-62` — único caller do caminho oficial. OK.
- 4 arquivos legados duplicam a mesma lógica e redefinem `NTFY_TOPIC = "sentinela"` literal:
  - `instagram_scraper/scrape_instaloader.py:18-31`
  - `instagram_scraper/scrape_instagram_final.py:18,31-33`
  - `instagram_scraper/scrape_instagram_system.py:19,32-34`
  - `instagram_scraper/scrape_instagram_system_final.py:19,32`
- `.env.example:51-53` documenta `NTFY_URL` e `NTFY_TOPIC` (default `https://ntfy.sh` e `sentinela`).
- Não existe `tests/test_ntfy_client.py` — nenhum teste cobre o cliente.

---

## Task 1: Fazer `ntfy_client.py` respeitar `NTFY_URL` do `.env`

**Objective:** O client deve montar a URL final a partir de `NTFY_URL` (base) + `NTFY_TOPIC` (path), em vez de hardcodar `https://ntfy.sh`.

**Files:**
- Modify: `core/ntfy_client.py:8-10`

**Step 1: Reescrever o cabeçalho do módulo**

Trocar o bloco:

```python
# Fallback para o tópico caso não exista no .env
NTFY_TOPIC = os.getenv("NTFY_TOPIC", "sentinela")
NTFY_URL = f"https://ntfy.sh/{NTFY_TOPIC}"
```

Por:

```python
# Fallbacks caso não existam no .env
NTFY_BASE_URL = os.getenv("NTFY_URL", "https://ntfy.sh").rstrip("/")
NTFY_TOPIC = os.getenv("NTFY_TOPIC", "sentinela")
NTFY_URL = f"{NTFY_BASE_URL}/{NTFY_TOPIC}"
NTFY_TOKEN = os.getenv("NTFY_TOKEN", "")  # opcional, para tópicos privados
```

**Step 2: Usar o token se existir**

Em `send_notification`, dentro de `headers`, adicionar `Authorization` quando `NTFY_TOKEN` estiver setado:

```python
headers = {
    "Title": title,
    "Tags": tags,
    "Priority": priority,
}
if NTFY_TOKEN:
    headers["Authorization"] = f"Bearer {NTFY_TOKEN}"
```

**Step 3: Validar manualmente que `NTFY_URL` montado bate com o esperado**

```python
import os
os.environ["NTFY_URL"] = "https://ntfy.exemplo.com"
os.environ["NTFY_TOPIC"] = "sentinela-test"
# Reimportar o módulo para recarregar constantes
import importlib, core.ntfy_client as n
importlib.reload(n)
assert n.NTFY_URL == "https://ntfy.exemplo.com/sentinela-test"
print("OK:", n.NTFY_URL)
```

Esperado: imprime `OK: https://ntfy.exemplo.com/sentinela-test`.

**Step 4: Commit**

```bash
git add core/ntfy_client.py
git commit -m "fix(ntfy): respeita NTFY_URL e NTFY_TOKEN do .env; corrige montagem hardcoded da URL"
```

---

## Task 2: Adicionar testes unitários para `ntfy_client`

**Objective:** Cobrir a montagem de URL e o envio real com `requests.post` mockado.

**Files:**
- Create: `tests/test_ntfy_client.py`

**Step 1: Criar o arquivo de teste**

```python
"""Testes para core.ntfy_client."""
import os
import importlib
from unittest.mock import patch, MagicMock

import pytest


def _reload_module(env: dict):
    """Recarrega core.ntfy_client com variáveis de ambiente controladas."""
    saved = {k: os.environ.get(k) for k in env}
    os.environ.update(env)
    try:
        import core.ntfy_client as ntfy
        importlib.reload(ntfy)
        return ntfy
    finally:
        for k, v in saved.items():
            if v is None:
                os.environ.pop(k, None)
            else:
                os.environ[k] = v


def test_default_url_quando_env_vazio():
    ntfy = _reload_module({})
    assert ntfy.NTFY_URL == "https://ntfy.sh/sentinela"


def test_url_customizada_quando_env_setado():
    ntfy = _reload_module({
        "NTFY_URL": "https://ntfy.exemplo.com",
        "NTFY_TOPIC": "meu-canal",
    })
    assert ntfy.NTFY_URL == "https://ntfy.exemplo.com/meu-canal"


def test_url_com_barra_no_final_e_normalizada():
    ntfy = _reload_module({
        "NTFY_URL": "https://ntfy.exemplo.com/",
        "NTFY_TOPIC": "canal",
    })
    assert ntfy.NTFY_URL == "https://ntfy.exemplo.com/canal"


def test_send_notification_monta_headers_corretos():
    ntfy = _reload_module({})
    with patch("core.ntfy_client.requests.post") as mock_post:
        mock_post.return_value = MagicMock(status_code=200)
        result = ntfy.send_notification(
            title="Teste",
            message="Olá",
            tags="robot",
            priority="high",
        )
        assert result is True
        mock_post.assert_called_once()
        args, kwargs = mock_post.call_args
        assert args[0] == "https://ntfy.sh/sentinela"
        assert kwargs["headers"]["Title"] == "Teste"
        assert kwargs["headers"]["Tags"] == "robot"
        assert kwargs["headers"]["Priority"] == "high"
        assert "Authorization" not in kwargs["headers"]


def test_send_notification_com_token_adiciona_bearer():
    ntfy = _reload_module({"NTFY_TOKEN": "tk_abc123"})
    with patch("core.ntfy_client.requests.post") as mock_post:
        mock_post.return_value = MagicMock(status_code=200)
        ntfy.send_notification("T", "M")
        kwargs = mock_post.call_args.kwargs
        assert kwargs["headers"]["Authorization"] == "Bearer tk_abc123"


def test_send_notification_retorna_false_em_erro():
    ntfy = _reload_module({})
    with patch("core.ntfy_client.requests.post", side_effect=Exception("timeout")):
        assert ntfy.send_notification("T", "M") is False
```

**Step 2: Rodar e verificar que passam**

```bash
pytest tests/test_ntfy_client.py -v
```

Esperado: 6 passed.

**Step 3: Commit**

```bash
git add tests/test_ntfy_client.py
git commit -m "test(ntfy): cobre montagem de URL, headers, token e tratamento de erro"
```

---

## Task 3: Deduplicar arquivos legados em `instagram_scraper/`

**Objective:** Substituir as 4 implementações locais de `send_ntfy_message` (ou equivalentes) por chamadas a `core.ntfy_client.send_notification`, eliminando as constantes `NTFY_TOPIC`/`NTFY_URL` redeclaradas como literal.

**Files:**
- Modify: `instagram_scraper/scrape_instaloader.py`
- Modify: `instagram_scraper/scrape_instagram_final.py`
- Modify: `instagram_scraper/scrape_instagram_system.py`
- Modify: `instagram_scraper/scrape_instagram_system_final.py`

**Step 1: Para cada arquivo, substituir a função local `send_ntfy_message` (ou bloco equivalente) por import do client central**

Em `scrape_instaloader.py` (linhas 18-33), trocar:

```python
NTFY_TOPIC = "sentinela"
NTFY_URL = f"https://ntfy.sh/{NTFY_TOPIC}"

def send_ntfy_message(title, message, tags="info", priority="default"):
    """Send a notification to ntfy"""
    headers = {
        "Title": title,
        ...
    }
    try:
        requests.post(NTFY_URL, data=message.encode('utf-8'), headers=headers)
    except Exception as e:
        print(f"Failed to send ntfy notification: {e}")
```

Por:

```python
from core.ntfy_client import send_notification as send_ntfy_message
```

E renomear `requests` se ele não for mais usado em outro lugar do arquivo.

Aplicar o mesmo padrão nos outros 3 arquivos. Cada arquivo terá:
- Removidas as linhas que definem `NTFY_TOPIC`/`NTFY_URL` como literal.
- Removida a função `send_ntfy_message` local.
- Adicionado `from core.ntfy_client import send_notification as send_ntfy_message` (mantém o nome para não quebrar callers).

**Step 2: Garantir que `requests` ainda é importado se houver outros usos**

```bash
cd C:\Projetos\sentinela
grep -n "requests\." instagram_scraper/scrape_instaloader.py
```

Se houver outros usos além do bloco ntfy, manter o import. Caso contrário, remover.

**Step 3: Verificar que os 4 arquivos ainda importam e rodam**

```bash
cd C:\Projetos\sentinela
python -c "import importlib; [importlib.import_module(f'instagram_scraper.{m}') for m in ['scrape_instaloader','scrape_instagram_final','scrape_instagram_system','scrape_instagram_system_final']]"
```

Esperado: nenhum ImportError.

**Step 4: Commit**

```bash
git add instagram_scraper/scrape_instaloader.py instagram_scraper/scrape_instagram_final.py instagram_scraper/scrape_instagram_system.py instagram_scraper/scrape_instagram_system_final.py
git commit -m "refactor(ntfy): deduplica client; instagram_scraper agora importa de core.ntfy_client"
```

---

## Task 4: Atualizar `.env.example` com `NTFY_TOKEN`

**Objective:** Documentar a variável `NTFY_TOKEN` no template, já que o client passou a suportá-la.

**Files:**
- Modify: `.env.example:51-53`

**Step 1: Substituir o bloco ntfy**

Trocar:

```
# --- NTFY NOTIFICATIONS ---
NTFY_URL=https://ntfy.sh
NTFY_TOPIC=sentinela
```

Por:

```
# --- NTFY NOTIFICATIONS ---
# Servidor ntfy (padrão oficial público). Use self-hosted alterando NTFY_URL.
NTFY_URL=https://ntfy.sh
# Tópico público no servidor acima. Para evitar spam de terceiros,
# use um nome aleatório (ex: sentinela-7f3a9b2c).
NTFY_TOPIC=sentinela
# Token de acesso (opcional). Necessário apenas se você criou um tópico
# privado em ntfy.sh ou se usa servidor self-hosted com auth.
# Em ntfy.sh: Settings -> Access Tokens -> Generate.
NTFY_TOKEN=
```

**Step 2: Commit**

```bash
git add .env.example
git commit -m "docs(ntfy): documenta NTFY_TOKEN no .env.example"
```

---

## Task 5: Verificação final end-to-end

**Objective:** Confirmar que tudo funciona junto com `.env` real.

**Files:** nenhum — só execução.

**Step 1: Garantir que `NTFY_TOPIC` existe no `.env` real**

```bash
cd C:\Projetos\sentinela
grep "^NTFY_" .env
```

Esperado: ver `NTFY_URL=...` e `NTFY_TOPIC=...` (o `.env` real não pode ser lido por este agente, mas o usuário pode confirmar).

**Step 2: Rodar o teste real de envio (dry-run com mock)**

```bash
cd C:\Projetos\sentinela
pytest tests/test_ntfy_client.py -v
```

Esperado: 6 passed.

**Step 3: Smoke test com `EventBus().notify`**

```bash
cd C:\Projetos\sentinela
python -c "
from core.event_bus import EventBus
ok = EventBus().notify('Sentinela Boot', 'Cliente ntfy configurado com sucesso', tags='white_check_mark', priority='default')
print('OK' if ok else 'FAIL')
"
```

Esperado: imprime `OK` (ou `FAIL` se o tópico exigir token não configurado — ambos são aceitáveis como evidência de que o código rodou).

**Step 4: Commit final se houver ajustes**

```bash
git status
# Se houver mudanças, commitar com mensagem descritiva.
```

---

## Arquivos modificados (resumo)

| Arquivo | Tipo | Motivo |
|---|---|---|
| `core/ntfy_client.py` | modify | Respeitar `NTFY_URL`, suportar `NTFY_TOKEN` |
| `tests/test_ntfy_client.py` | create | 6 testes cobrindo URL, headers, token, erro |
| `instagram_scraper/scrape_instaloader.py` | modify | Remover client local, importar de `core` |
| `instagram_scraper/scrape_instagram_final.py` | modify | Remover client local, importar de `core` |
| `instagram_scraper/scrape_instagram_system.py` | modify | Remover client local, importar de `core` |
| `instagram_scraper/scrape_instagram_system_final.py` | modify | Remover client local, importar de `core` |
| `.env.example` | modify | Documentar `NTFY_TOKEN` |

## Critérios de aceitação

- [ ] `NTFY_URL` no `.env` é respeitado (URL final = base + topic).
- [ ] `NTFY_TOKEN` no `.env` adiciona header `Authorization: Bearer ...`.
- [ ] 6 testes em `tests/test_ntfy_client.py` passam.
- [ ] Nenhum dos 4 arquivos legados redefine `NTFY_TOPIC` literal.
- [ ] `EventBus().notify(...)` continua funcionando sem mudança de assinatura.

## Riscos e tradeoffs

- **Risco 1:** Mover a constante `NTFY_URL` de literal para env pode mudar o comportamento padrão se o `.env` real tiver `NTFY_URL` apontando para outro servidor. Mitigação: a tarefa explicita que `NTFY_URL` já existe documentado em `.env.example:52`, então o `.env` real provavelmente já tem valor coerente.
- **Risco 2:** Os 4 arquivos legados em `instagram_scraper/` parecem ser versões antigas/experimentais (sufixos `_final`, `_system`, etc.). Podem estar mortos. Mitigação: a deduplicação é segura mesmo se estiverem mortos — só remove código duplicado.
- **Tradeoff:** Adicionar `NTFY_TOKEN` é um plus não pedido explicitamente, mas o suporte já era trivial (1 linha) e o `.env.example` ficaria inconsistente sem documentar. Vale a pena pelo YAGNI invertido: custa 1 linha e evita retrabalho depois.

## Open questions

- Nenhuma no escopo "só consertar config". Se o usuário quiser tópicos privados depois, a Task 1 já deixa a infraestrutura pronta.